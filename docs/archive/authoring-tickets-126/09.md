# T09：补齐文本、字段文本和地图组件的语言创建

状态：拆分已获认可，已发布为 [#135](https://github.com/CCharlesMeng/MetricCanvas/issues/135)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

内容 MCP 能根据受控请求创建 text、fieldText、mapChart 并在统一运行时正确呈现。

责任：C1 主责，按独立构造器划定修改范围。

## Acceptance criteria

- [ ] 三类组件必需字段、数据形状和地图语义满足既有协议，缺配置不伪造成功。
- [ ] 覆盖创建、加入已有页面及删除后的引用校验；保持其他内容不变。
- [ ] 通过公开工具生成的页面经产品校验，并对真实代表性数据验证呈现。

## Blocked by

T08。

## 与既有规划的关系

新增缺失创建路径，不扩大组件目录，也不建设手工添加入口。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
