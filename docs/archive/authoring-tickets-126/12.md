# T12：独立生命周期 MCP 保存并读取精确草稿

状态：拆分已获认可，已发布为 [#138](https://github.com/CCharlesMeng/MetricCanvas/issues/138)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

可信调用方能通过独立 MCP 保存页面、读取精确修订和历史，获得可交给前端的引用。

责任：C5 主责。

## Acceptance criteria

- [ ] 内容与生命周期两入口独立装载；生命周期工具不执行页面编辑或参数提取算法。
- [ ] 保存使用已校验文档、基线与稳定幂等标识，返回精确修订；验证重复、结果不确定及冲突行为。
- [ ] 提供精确读取、历史读取所需工具和受控身份注入；外部未提供能力时明确不可用，不能以 latest 冒充指定旧修订。
- [ ] 工具端口以 T04 方案验证；已知 HTTP 契约按资料适配，拟新增端点不得伪装成生产实现。

## Blocked by

T02, T04。

## 与既有规划的关系

消费 #105 资料，外部语义实现不在本票；不恢复 #125 删除的旧服务端 MCP。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
