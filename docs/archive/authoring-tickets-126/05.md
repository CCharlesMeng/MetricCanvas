# T05：迁移浏览器消费端到 layout 规范形式

状态：拆分已获认可，已发布为 [#131](https://github.com/CCharlesMeng/MetricCanvas/issues/131)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

让页面在画布、正式运行时与 Embed 中使用新版布局表示，仍能打开约定支持的旧页面。

责任：C0/C3 主责，C2 仅消费规范页面。

## Acceptance criteria

- [ ] 浏览器页面构造、读取、渲染和示例消费按 T03 规范迁移，旧兼容入口保留至收口。
- [ ] 布局形态、组件占位及看板/报表行为不变；兼容读到的页面只以规范形式交给后续新流程。
- [ ] 校验正式渲染、搭建画布和仓外 Embed 消费的公开行为，批次完成时检查保持通过。

## Blocked by

T03。

## 与既有规划的关系

复用既有两种引擎集成通道；真实异构集成仍由 #103 负责。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
