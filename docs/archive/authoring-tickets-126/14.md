# T14：断网或重开后恢复并安全重试创作工作

状态：拆分已获认可，已发布为 [#140](https://github.com/CCharlesMeng/MetricCanvas/issues/140)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

用户断网继续编辑并重新打开后能恢复本地工作，联网后按原顺序完成同步。

责任：C2 同步协调主责。

## Acceptance criteria

- [ ] IndexedDB 按用户、工作区、页面隔离工作副本和队列，不保存运行凭据。
- [ ] 覆盖页面重开、重连、退避重试、服务端已提交但回执丢失；稳定操作标识重用，不重复建修订。
- [ ] 校验失败、身份失效和冲突暂停相应同步并保留内容；本地存储失败不能显示已保护。
- [ ] 待同步时允许手工编辑；进入语言修改和发布须等待同步完成；验证跨用户切换不会恢复错内容。

## Blocked by

T13。

## 与既有规划的关系

创作恢复与 #108 的对话历史/分析检查点分开，本票不实现会话库。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
