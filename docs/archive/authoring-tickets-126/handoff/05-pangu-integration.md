工作项目：/Users/moon/Documents/Code/公司项目/DataDashboard。先只读读取 /Users/moon/Documents/Code/公司项目/DataDashboard/docs/archive/authoring-tickets-126/handoff/common.md 和 /Users/moon/Documents/Code/公司项目/DataDashboard/docs/archive/authoring-tickets-126/development-plan.md，再读取执行仓 AGENTS.md、领域词汇表、相关 ADR，以及 GitHub CCharlesMeng/MetricCanvas 的 #126 和本角色负责票的完整正文、最新评论与依赖。新 worktree 若没有未提交交接文件，从上述原始路径读取；保留原工作区及其他会话改动。遵循 common.md 的独立 worktree、共同基线、所有权、验收和等待约定。

你是 S5，负责盘古独立模块的真实实例/事件/配置适配，在 M0 READY 后启动。先从 S1 接收 #127 的模块、稳定接口、独立运行示例和所有权清单；工作台组装及创作工作副本仍由 S1 负责。你不直接改画布状态、自动保存或 Java 生命周期规则。

接续 #106/#107/#108 的当前任务边界。先读完整正文及最新有证据评论：#108 本地保留旧页/取消/迟到结果隔离已经做完，真实 SDK/身份、产物读取/完整性和恢复仍需确认。根据现有资料和当前环境核实实例 API、受控工具路由、身份、确认承接、取消、历史及程序产物通道。具体实现细节在本模块实施时集中 grill；产品已定问题不重问，未知提供方事实不编造。

在独立入口完成真实盘古适配，再按 #127 稳定接口接入工作台。全部对话由盘古呈现，画布和检查器保持原位；缺真实细粒度事件时只显示整体处理中。新轮失败、取消、等待或纯文本结果保留旧页；停止本地接收与服务端停止分别验证。完整页面与数据行不经模型文本回传，customRemoteCard 不承担本版页面产物通道。

#146 在 S1 侧完成后，与其验证本版创作路径：Relay 通过内容和生命周期工具保存，只向前端交付可验证的精确草稿引用，创作流程鉴权读取后更新画布。真实交接需要 #138/#146 及提供方相应能力，基础对话适配不必等这些完成。参数候选发布对话需与 #145 协议对齐，不将任意肯定答复解释为发布授权。

上游源码、独立适配和部署配置分离。按已确认集成方式实际演练兼容的上游代码更新，验证无需手工补改工作台/上游源码即可恢复集成。破坏性 SDK 变更集中升级适配层。保持静态 SPA 与既有配置注入，不新增 Node 服务或默认微前端协议。

本提示词允许你在自己负责的 #106/#107/#108 记录已核实事实和本次证据，并向已登记的 S0/S1 发送交接；不代发外部提供方消息。若环境或接口事实不足，继续可独立验证的适配工作，交出“具体缺哪项资料/哪种环境/阻塞哪项验收”，结束本轮等待统筹续跑，不能宣称真实接通。聊天历史、分析检查点和创作草稿恢复分别验收，不能用其中一项通过替代另一项或关闭整张旧票。
