# T17：用 params 初始化页面并保持后续筛选独立

状态：拆分已获认可，已发布为 [#143](https://github.com/CCharlesMeng/MetricCanvas/issues/143)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

模板的受控维度单值/多值可初始化具体查询与筛选；用户随后筛选不被原始参数覆盖。

责任：C0/C3 主责；Python 仅消费生成契约。

## Acceptance criteria

- [ ] 冻结显式查询目标与筛选初始化绑定，默认来源唯一，不新增 globalParams/dimensionValues 根字段。
- [ ] 用一个代表性模板走通公开校验、实例化与统一运行时/Embed 呈现，覆盖部分数据源共享和缺值。
- [ ] URL 仅初次读取，后续交互读当前 filters；不监听 URL，不将运行值写回模板。
- [ ] 旧标量参数与文本取值继续可用，跨语言契约及版本门禁一致；不引入时间/金额或通用表达式。

## Blocked by

T07。

## 与既有规划的关系

延续 #95 已交付的页面参数、导航和统一运行时，不重建渲染器。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
