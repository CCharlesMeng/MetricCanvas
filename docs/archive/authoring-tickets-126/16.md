# T16：手工调整已有组件的白名单属性并保存

状态：拆分已获认可，已发布为 [#142](https://github.com/CCharlesMeng/MetricCanvas/issues/142)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

用户在属性面板调整已支持的指标卡、图表和表格属性，并自动保存有效修改。

责任：C2 属性面板主责，独占属性编辑界面。

## Acceptance criteria

- [ ] 按 #126 白名单实现 reportHeader、metricCard、barChart、lineChart、pieChart、table 的已有属性，不开放任意 JSON 路径。
- [ ] 每次完整合法属性操作进入 T13 同步流程，无变化不保存，不覆盖同组件其他设置。
- [ ] 通过界面验证属性修改、渲染效果与读回修订一致；手工添加和原始 JSON 编辑保持不开放。

## Blocked by

T13。

## 与既有规划的关系

不因 #95 旧文档写“人工十类构造”就推断存在手工添加 UI。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
