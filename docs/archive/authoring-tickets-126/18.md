# T18：消费执行结果完成初始化、预览及筛选历史接入

状态：拆分已获认可，已发布为 [#144](https://github.com/CCharlesMeng/MetricCanvas/issues/144)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

按精确页面或候选引用执行并渲染，查询数据、实际取值和筛选显示一致，后续交互继续前端查数。

责任：C3 执行适配主责，C2 仅提供页面装载点。

## Acceptance criteria

- [ ] 消费执行结果的成功与失败项，正确使用匹配条件的内嵌初始行，失败映射既有数据快照错误并保留成功部分。
- [ ] 通过外部契约验证显式输入、历史、默认、权限回退优先级；无权限范围不产生不受限查询。权限判断的权威实现仍在外部服务。
- [ ] 支持创作精确预览及 IOC 初始化消费；初始化后筛选走既有数据网关，每次更新对应最后筛选记录。
- [ ] 确定性测试和真实提供方验收分别记录；未确认接口形状不假装在线可用。

## Blocked by

T02, T04, T17。

## 与既有规划的关系

本仓消费端增量；真实 IOC/异构集成沿 #103，静态页面真实部署沿 #104。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
