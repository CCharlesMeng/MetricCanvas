# T02：将现有草稿读写接入独立创作协调边界

状态：拆分已获认可，已发布为 [#128](https://github.com/CCharlesMeng/MetricCanvas/issues/128)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

保持现有打开、手工修改、保存和精确预览可用，同时让同步、草稿读取与对话结果消费可独立替换。

责任：C2 主责，C0 管理新增公开契约。

## Acceptance criteria

- [ ] 现有页面资产客户端仍消费已确认 HTTP 接口，不恢复 Node 同源回退或服务端路由。
- [ ] 抽出工作副本、精确修订读取、保存状态与对话交接接口；现有手工保存通过该边界完成浏览器回归。
- [ ] 冻结页面身份、修订、操作关联、保存结果及冲突的内部最小契约，区分外部已支持、待提供和内部提案。
- [ ] 指定工作台共享接入点和各模块独占修改范围；历史不可用继续明确呈现，不用当前修订伪装历史。

## Blocked by

无，可立即开始。

## 与既有规划的关系

#104 静态化与 #120 页面资产客户端已完成，直接复用；不重复 #125 旧链清理。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
