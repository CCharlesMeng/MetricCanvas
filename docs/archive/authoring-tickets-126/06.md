# T06：迁移 Python 页面产物到 layout 规范形式

状态：拆分已获认可，已发布为 [#132](https://github.com/CCharlesMeng/MetricCanvas/issues/132)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

让 Python 工具输出新版合法页面，并按约定读取旧基线进行编辑。

责任：C1 主责；C0 提供只读契约。

## Acceptance criteria

- [ ] Python 解析与构造使用 T03 冻结的规范形式，不手改产品协议快照。
- [ ] 既有组件构造和完整页面校验通过，输出与浏览器共享黄金向量一致。
- [ ] 旧基线支持范围与浏览器一致，协议外版本给出稳定失败，不猜测字段替代。

## Blocked by

T03。

## 与既有规划的关系

沿用 #89 已有确定性 Python 与 Bundle 分发能力，旧票未勾选状态不作为重建理由。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
