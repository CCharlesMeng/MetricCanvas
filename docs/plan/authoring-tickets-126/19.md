# T19：界面与生命周期 MCP 完成候选确认发布

状态：拆分已获认可，已发布为 [#145](https://github.com/CCharlesMeng/MetricCanvas/issues/145)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

从精确草稿准备参数提取候选，经纠正、预览和人工确认发布不可变模板，两种入口遵循同一规则。

责任：C2 发布界面主责，C5 发布工具主责；共同契约一次冻结。

## Acceptance criteria

- [ ] 外部 Java 提取和校验；界面呈现候选差异、作用范围、是否保留维度取值，生命周期 MCP 提供对应操作。
- [ ] 只处理等值/多值及部分共享，排除同维度不同取值共享与时间/金额范围；不复制 Java 算法到前端或工具。
- [ ] 人工确认关联候选和来源修订，候选过期、基线变化、未确认或租约失效不得发布；内容修改回草稿后重新准备。
- [ ] 发布后草稿修改不改运行版本；通过 UI 与工具公开场景分别验证同一契约，真实服务未实现时明确依赖。

## Blocked by

T12, T14, T18。

## 与既有规划的关系

#126 明确新增的创作发布前端；覆盖 #95 早期“移除发布界面”的本版范围，但不恢复旧模板库/ACL/Node 后端。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
