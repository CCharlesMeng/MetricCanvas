# T10：补齐容器与动态总结组件的语言创建

状态：拆分已获认可，已发布为 [#136](https://github.com/CCharlesMeng/MetricCanvas/issues/136)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

内容 MCP 能创建 tabContainer、compositeCard、aiSummary，完整生成合法子树或动态总结定义。

责任：C1 主责，构造器范围与 T09 分离。

## Acceptance criteria

- [ ] 容器子树遵守现行子组件限制，创建和删除不留悬垂引用。
- [ ] aiSummary 仅在明确要求运行时流式总结且具备有效配置时创建，普通摘要输出 text。
- [ ] 用公开工具验证合法构造和缺配置失败；与 T08/T09 合计覆盖全部 17 类组件，不能以空壳或假端点达标。

## Blocked by

T08。

## 与既有规划的关系

复用现有容器和 AI 总结渲染能力，助手侧边栏与 aiSummary 为两个模块。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
