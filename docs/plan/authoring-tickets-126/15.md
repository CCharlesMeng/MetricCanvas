# T15：撤销编辑并恢复任意可读取历史修订

状态：拆分已获认可，已发布为 [#141](https://github.com/CCharlesMeng/MetricCanvas/issues/141)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

用户撤销上一步或选择历史版本恢复后，得到新的页面修订并保留原历史。

责任：C2 历史与工作副本主责。

## Acceptance criteria

- [ ] 撤销针对完整操作，历史展示与精确读取使用生命周期端口；不可用能力明确提示。
- [ ] 恢复通过页面校验并作为新操作保存，已发布版本不受影响。
- [ ] 存在未确认保存时先核实结果，不删除可能已提交的队列项；冲突不得自动覆盖。
- [ ] 验证恢复旧修订、撤销未同步操作及重开后的历史继续编辑，不以聊天历史冒充页面历史。

## Blocked by

T14。

## 与既有规划的关系

使用 T02/T04 端口，不依赖生命周期 MCP 已完成，因为 UI 不绕道调用 MCP。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
