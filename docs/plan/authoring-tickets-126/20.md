# T20：按已保存草稿引用完成语言创作交接

状态：拆分已获认可，已发布为 [#146](https://github.com/CCharlesMeng/MetricCanvas/issues/146)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

语言创建或改版完成后只交付保存引用，工作台读取对应修订并更新画布，完整文档不经模型回传。

责任：C2 创作交接主责；C1/C5 只消费各自稳定接口。

## Acceptance criteria

- [ ] 以确定性 Relay 端口替身走通内容编辑→生命周期保存→引用通知→精确读取→校验→画布更新，首次合法产页后才建资产。
- [ ] 语言修改前等待本地同步，期间禁用当前手工修改；失败、取消、文本或等待确认保留已有页面。
- [ ] 复用 #108 本地状态隔离，覆盖迟到、重复、其他窗口基线冲突、读取失败及权限失效；不解析不透明 ID 判断顺序。
- [ ] 部分成功只保存合法子集一次，并显示真实操作结果；保存失败不得把未保存产物当作精确修订。
- [ ] 独立对话模块通过 T01 接口联入；真实盘古/Relay 的事件映射、身份及程序产物验证沿 #107/#108 执行并提供创作场景证据。

## Blocked by

T01, T08, T12, T14。

## 与既有规划的关系

本票只增量建设创作草稿交接，不复制 #108 普通问数的检查点恢复；真实服务接线不可用不阻塞端口级验证。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
