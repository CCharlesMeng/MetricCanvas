# #126 向 #103/#104 原执行线的增量交接

2026-09-14。当前为文档基线，**没有新版本运行产物或真实验收**。不把 #95 既有成果重做为 #126 成果。

来源：`docs/plan/wayfinder-103-intranet-resume.md`（已明确迁入内网后执行）、`docs/plan/wayfinder-103-start-handoff-2026-09-09.md`、`docs/reviews/2026-09-09-103-release-integration.md`、#104 最新静态化评论（8ee924d）。#104 未发现单独新版内网 handoff，沿用该评论的部署/配置/CORS/身份/真实盘古门槛，不虚构环境可用。

| 项目 | 可复用 | 新版本需交接 | 当前状态 |
|---|---|---|---|
| #103 四包 | 既有 pack、仓外消费和浏览器矩阵；旧 1.0.0-rc.1 包有独立摘要 | #133 后确定的新 schema 支持区间、四包版本/精确集成 SHA/tarball 摘要；#144 后执行输入/错误场景 | 等本版实现，当前不重新打旧包冒充新包 |
| #104 Platform | adapter-static、runtime-config、page-assets-client、静态 artifact CI | M1 后新静态产物 SHA、运行配置要求、保存/恢复/语言交接；M2 后发布场景 | 真部署仍待原内网环境 |
| #106/#107/#108 | 本地 analysis-page-state 与 11 项既有测试；指南事实 | S1 #127 模块经明确文件交接给 S5；真实路由/用户/引用通道/取消/恢复/更新演练 | M0 后 S5 接续，未登记 |
| #125 | 旧 Node、服务端 MCP/模板链已退场及已有门禁 | 只核对残留消费者及发布产物不带入旧链 | 不恢复旧后端 |

新增验收场景：

1. M0 新旧 report/dashboard 文档在真实 Embed 入口等价；未知协议失败关闭；Python 与四包支持区间一致。记录实际 registry/精确版本，不能沿用旧 tarball 哈希。
2. M1 当前页面手工修改→同步→语言局部修改→内容工具→生命周期保存→只传引用→按用户精确读取→画布更新。断网/回执丢失/重复/冲突/取消/迟到保留内容；完整文档不进模型文本流。
3. M2/M3 params 一次初始化与 filters 后续变化、权限回退/零权限、初始行条件匹配、空集/部分失败；候选修正、预览、人工绑定确认、过期/基线变化拒绝发布；真实执行不自动产生持久化页面。
4. 盘古上游兼容更新前后使用相同配置与上述场景回归，记录上游前后版本、适配提交与配置来源，不能把本地替身演练等同真实更新通过。

原执行线续跑文字：

> 在内网原 #103/#104 执行环境读取本文件和旧 intranet-resume。先取 S0 公布的具体集成 SHA、发布清单和摘要；当前仅文档基线时不要发布。复用已有 pack/安装/浏览器流程，验证本版新增场景并分别回传本仓、外部确认、真实联调证据。发布 registry/版本授权沿原规则，不覆盖现有版本。缺少真实环境时记录等待，不在本机另造应用替代真实终点。

此文字尚未发送任何旧任务；只有本版角色实际登记且授权边界明确后使用任务消息。原内网条件沿已有交接等待，不再次索要本机无法提供的环境。#126 达标不自动关闭 #95 的普通问数和完整分析会话恢复目标。
