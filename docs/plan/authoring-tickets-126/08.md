# T08：内容 MCP 对既有页面执行受控改版

状态：拆分已获认可，已发布为 [#134](https://github.com/CCharlesMeng/MetricCanvas/issues/134)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

通过内容 MCP 创建既有构造器支持的页面，或局部修改完整基线，并返回可保存的合法成功子集。

责任：C1 主责。

## Acceptance criteria

- [ ] 复用已有十类数据组件及 reportHeader 构造，不重做发现、DQE 验真和组页。通过公开工具支持标题、布局、受形状约束的类型及白名单属性修改。
- [ ] 保持所有未触及手工内容；编辑输入由可信程序提供完整基线，模型只得到必要摘要。
- [ ] 独立操作失败回滚、依赖项跳过、成功子集整页校验；全失败和无变化明确报告，不生成保存副作用。
- [ ] 独立内容 MCP 入口及结构化错误/操作结果可通过 stdio 或公开工具调用验证，注册扩展方式供后续组件任务复用。

## Blocked by

T02, T06。

## 与既有规划的关系

替代本版创作路径中旧 build_page 混合保存角色；复用 #89/#91 已完成部分，普通问数不迁移。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
