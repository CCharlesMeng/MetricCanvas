# T11：语言修改维度筛选及表格链接下钻

状态：拆分已获认可，已发布为 [#137](https://github.com/CCharlesMeng/MetricCanvas/issues/137)，标签 ready-for-agent；尚未启动实现。

## Parent

[规格 #126](https://github.com/CCharlesMeng/MetricCanvas/issues/126)；关联规划 [#95](https://github.com/CCharlesMeng/MetricCanvas/issues/95)。

## What to build

用户通过语言修改页面的维度筛选、查询绑定及表格链接下钻，生成可运行的交互。

责任：C1 主责，独占交互操作模块。

## Acceptance criteria

- [ ] 新增/修改/移除筛选及显式绑定作为完整操作，避免悬垂引用，未触及数据源保持不变。
- [ ] 表格链接下钻复用 href 与显式参数传递，在页面生成后经统一运行时触发验证。
- [ ] 支持部分成功语义，查询分页排序及表头筛选继续拒绝，不借属性编辑开放。

## Blocked by

T08。

## 与既有规划的关系

复用已关闭 #109 的导航能力；本票只补创作操作，不重做导航引擎。

共同约束：只修改本仓授权范围；Java/Relay 仅给修改方案和消费适配。共享代码由指定所有者改动；测试替身位于外部边界，明确区别内部提案与真实提供方契约。继承 #126 的范围排除和验证约定。
