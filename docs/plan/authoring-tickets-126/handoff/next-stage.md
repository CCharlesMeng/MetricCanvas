# #126 下一阶段执行 handoff

状态：用户已审查通过本仓17票、参考手册与rc.4矩阵，并授权本批远端交付和Issue状态更新。此文安排下一阶段，不宣称后续人员已就位或真实服务可用。

## 先定位唯一基线

已验收产品基线为 `f15c708a22ee95d9f6ca0617c63997d7ab8db337`，交付分支为 `codex/authoring-126-integration`。本文件随本批PR进入main后，新开发者从包含该提交的最新 `origin/main` 建立独立worktree，向S0报告实际SHA；若尚未合并，使用本交付分支的远端HEAD并核对其包含上述基线。不要继续从旧 `codex/issue-127-pangu-handoff` 或9票 `codex/authoring-126-delivery` 开始。

依次读取 [common.md](common.md)、[development-plan.md](../development-plan.md)、执行仓AGENTS.md/CONTEXT.md/ADR索引，以及#126、负责票和依赖的完整正文及最新评论。旧角色文档提供职责背景；本页的阶段和基线更新优先。原工作区 `/Users/moon/Documents/Code/公司项目/DataDashboard` 的未提交改动保持原位。

完成开工的条件：独立worktree和codex/分支建立；报告角色、真实任务ID、SHA和精确文件清单；S0将其写入唯一台账 [coordination.md](../coordination.md)。目前S4/S5尚未登记，不创建新任务或代联外部提供方。

## S4：现在启动 #138，随后参与 #145

将以下文字连同本文件交给用户开启的S4任务：

> 你是S4，先读本页及04-lifecycle-mcp.md，在独立worktree从正式共同基线执行#138。#128/#130已验收，M0 READY，不再等待M0。先核对t04-java-relay-proposal.md、t04-contract-examples.json、t02及t13/t14/t18的消费边界，登记独立生命周期MCP入口、application/port/adapter、自有输入契约及测试的精确文件后实施。内容MCP和工作台客户端由其他角色维护。

#138须提供独立保存/精确读取/历史工具；稳定操作标识、身份、基线冲突、未知结果查询、重复提交、精确读取不回退latest。完整文档沿可信程序通道传递，模型只见摘要/引用。已知HTTP按#105事实消费；未来Java契约用显式端口验证，不发明生产端点。保存幂等/回执丢失/越权/冲突/历史不可用必须有正反例和公开生产stdio及隔离安装证据。

回执必须列作者/生成/证据SHA、本仓测试、外部确认、真实联调和未实现项。S2唯一生成快照/锁；S0验收集成后发布可消费SHA，才解锁S1的#146。

## S1：#138集成后优先 #146，再汇合 #145

现有任务ID `01a09f69-8346-7e91-9c66-9e091d3f6e77`，保留现有任务续跑，不创建替代任务。

> #138未集成时保持等待。收到S0正式SHA后核对#127/#134/#138/#140与消费兼容，优先执行#146。真实语言修改产物保存后只传引用，鉴权精确读回画布；失败、断网、取消、迟到事件和纯文本结果保留旧页，未知保存不得伪称成功。代表性页面走通后提交M1完整证据。

#145前置#138/#140/#144齐备后，由S1负责发布界面和整票最终验收，S4完成工具。候选内容/基线/身份与人工确认绑定；旧候选、过期响应、基线变化及未确认均不能发布。自然语言发起发布不自动成为当前候选的确认。

#145文件边界预分配（正式开工仍报实际文件）：S1唯一修改 `apps/platform/src/lib/page-assets-client.ts`、`PageAuthoringWorkbench.svelte`、`RevisionPreview.svelte`及workbench发布UI/浏览器测试；S4唯一修改其新生命周期MCP模块/工具注册/服务适配与测试；S2唯一修改公共产品契约、导出入口、生成快照及锁。S4不能直接改工作台客户端或公共导出；需要新增共用契约先交S0/S2冻结。S3内容MCP及page-builder Skill主文保持原所有权。

## S5：现在可做真实盘古适配，完整创作联调等 #138/#146

将以下文字交给用户开启的S5任务：

> 你是S5，先读本页及05-pangu-integration.md，接续#106/#107/#108既有事实。从新正式main独立开发，不沿旧#127快照继续分叉。向S0登记真实任务ID并从S1接收runtime.ts/PanguDialogue.svelte的精确文件交接后实施；port.ts、工作台协调和保存规则仍由S1维护。核验真实SDK实例API与部署环境，缺事实列清单，不改外部服务或联系提供方。

接入定案：`window`全局 `metriccanvas:draft-saved` 事件detail仅 `{draftId}`；部署注入 `window.__METRICCANVAS_PANGU__={resourceUrl,version}`。SDK/适配/部署配置分离，同一文档固定SDK版本，换版本刷新。凭据仅运行内存，不能进入文档/持久化/模型摘要。draftId不可擅自等同pageId或revisionId，真实精确地址能力未确认时保持不可用。

所有权状态：本批交付时两文件仍由S1保管；S5实际登记且S1给出无并行修改回执后，S0台账将两文件正式移交。未登记人员不会被宣称已获文件。

验收真实SDK加载/卸载、事件重复与重试、取消和迟到隔离、身份切换、上游SDK升级后重集成。真实草稿交接联合#138/#146；发布确认联合#145。区分停止本地接收与服务端停止。#108本地旧页保护和#104静态化直接复用。

## S0与阶段关口

S0任务 `01a09f5f-0882-7762-b736-c3ed8b5504e8`；S2 `01a09f69-a06b-7703-b87b-ccdfe05d765e`；S3 `01a09f69-d2b5-71b0-ba93-c7cc183d4ee2`。台账仅S0写，各票须验收集成后通知下游，以集成SHA作为同一可消费基线。S2负责后续共同契约/手册和统一生成，S3按具体内容问题提供支持，不重开已完成票。

| 关口 | 当前状态/完成条件 |
|---|---|
| M0 | 已通过：#127–#133本仓架构与兼容 |
| M1 | 待#138/#146：代表页语言修改→保存→只传引用→读回画布，包含断网/失败 |
| M2 | 待#138/#145/#146整票和最终组合回归；手册及rc.4分发矩阵已有固定证据 |
| M3 | 待真实盘古/Java/Relay/DQE、#103异构宿主、#104内网部署和上游更新后集成 |

#130只完成方案；真实强幂等/历史/候选/执行权威尚需服务确认。#95、#103–#108、#125保持各自未完成范围，不因本批17票关闭而关闭。#125仅核对遗留清理，不恢复旧后端。

所有回执分别列“本仓 / 外部确认 / 真实联调”。本地HTTP/SSE和外部替身不算真实服务。逐variant手册例子仅结构语义验证，视觉边界仍见reference-evidence.md。无可做工作时有界等待后清楚交接，不承诺自动后台继续。
