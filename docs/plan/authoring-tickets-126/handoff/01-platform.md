工作项目：/Users/moon/Documents/Code/公司项目/DataDashboard。先只读读取 /Users/moon/Documents/Code/公司项目/DataDashboard/docs/plan/authoring-tickets-126/handoff/common.md 和 /Users/moon/Documents/Code/公司项目/DataDashboard/docs/plan/authoring-tickets-126/development-plan.md，再读取执行仓 AGENTS.md、领域词汇表、相关 ADR，以及 GitHub CCharlesMeng/MetricCanvas 的 #126 和本角色负责票的完整正文、最新评论与依赖。新 worktree 若没有未提交交接文件，从上述原始路径读取；保留原工作区及其他会话改动。遵循 common.md 的独立 worktree、共同基线、所有权、验收和等待约定。

你是 S1，负责工作台组装与创作协调。先执行 #127（T01）→#128（T02），两票独立验收、连续开发。目标是在保留现有打开/编辑/保存/精确预览行为的同时，拆出可独立运行的盘古对话模块和可替换的创作协调边界。

先与 S0 登记工作台/客户端/同步模块文件所有权。盘古模块目录、加载与配置保存机制在 #127 实施时集中 grill；已经确认的对话/画布职责不重问，真实 SDK 事实读 #106。#127 先用明确的内部端口与替身验证独立运行、失败保留旧页及兼容上游更新。复用 #108 的已有状态隔离，不重写它。#128 继续使用真实已确认的页面资产客户端，不恢复 Node 路由，历史能力缺失明确呈现。

提交 #127/#128 的本地 commit、接口和浏览器证据给 S0。M0 后把盘古独立模块的适配实现所有权交给 S5，工作台接入点仍由你负责。收到 M0 READY 并取得集成基线前，只继续非依赖实现的核验工作。

M0 后推荐顺序：#139（T13）自动保存→#140（T14）浏览器恢复；#127/#134/#138/#140 的实现集成后优先做 #146（T20）语言创作交接；随后 #141（T15）撤销历史、#142（T16）属性白名单。#142 技术上只依赖 #139，若有独立属性面板负责人可由 S0 调配。保存与恢复共用一个有序队列，不在不同 UI 自建保存机制。

#146 只消费内容 MCP、生命周期 MCP 和对话端口：生成合法页面后保存，收精确引用再读回，验证迟到/取消/读取失败不覆盖已有内容。代表性组件足够打通首条闭环，不等 #135–#137 全部完成。真实盘古/Relay 适配由 S5 接续，边界替身通过不声称已接通。

#138/#140/#144 验收并集成后，你主责 #145（T19）的候选差异、修正、精确预览、人工确认与发布反馈。与 S4 约定发布工具的独占范围，共用候选/确认语义；你负责整票最终组合验收。参数提取和发布裁决在外部 Java，不在前端复制算法。

验收至少覆盖：每个完整操作一次保存、网络延迟和重复回执、存储失败/断网重开/身份隔离、撤销恢复新修订、未触及手工设置保留、候选失效和未确认不能发布。界面改动提供浏览器证据，按需要使用仓库 SDD。每票交付并等共同基线就绪后续跑，不跨修改 S2 的协议、S3 的内容实现或 S5 的盘古专有适配。
