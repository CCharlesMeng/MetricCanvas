工作项目：/Users/moon/Documents/Code/公司项目/DataDashboard。先只读读取 /Users/moon/Documents/Code/公司项目/DataDashboard/docs/plan/authoring-tickets-126/handoff/common.md 和 /Users/moon/Documents/Code/公司项目/DataDashboard/docs/plan/authoring-tickets-126/development-plan.md，再读取执行仓 AGENTS.md、领域词汇表、相关 ADR，以及 GitHub CCharlesMeng/MetricCanvas 的 #126 和本角色负责票的完整正文、最新评论与依赖。新 worktree 若没有未提交交接文件，从上述原始路径读取；保留原工作区及其他会话改动。遵循 common.md 的独立 worktree、共同基线、所有权、验收和等待约定。

你是本版统筹会话 S0。目标是让 #127–#146 在不重做 #95 成果的前提下按架构前置、模块并行和两处汇合推进。请现在开始统筹及 #130（T04）Java/Relay 修改方案，不实施其他会话的模块代码。

先准备共同基线：识别并只纳入本版规格、ADR-0078、相关词汇变更、票稿及 handoff；其他工作区改动逐项排除。建立独立集成 worktree 和本地集成分支，公开基线 SHA。建立 coordination.md 台账（仅你写）记录会话 ID、分支、文件所有权、前置、验收与集成提交。开发者尚未就位时仍可完成 #130 和基线准备，不凭空宣布已分配人员。

首批角色为 S0/S1/S2/S3。S1 做 #127→#128；S2 做 #129→#131，等待 S3 的 #132 后做 #133；S3 在 #129 的版本契约进入共同基线后做 #132。你同步完成 #130，明确 Java/Relay 现有能力、拟新增契约和未知事实，不改外部服务。消费契约与 S1/S2 对齐，至少有保存幂等/精确读取、发布候选和执行结果的完整正反例。

#127–#133 全部按各自范围验收并集成后，你发布 M0 READY（列全套 commit 和兼容证据），再让用户开启 S4/S5，并续跑 S1/S2/S3 的功能任务。不要用等待 M0 阻止 M0 自身的 #132/#133。

M0 后：S1 做保存恢复及创作交接；S2 做参数与执行；S3 做内容编辑与组件覆盖；S4 做生命周期工具；S5 接续盘古真实适配。优先解锁 #146：#127/#134/#138/#140 都就绪后通知 S1。#145 等待 #138/#140/#144，由 S1 主责发布界面与最终验收、S4 完成发布工具。分配 #145 内的文件所有权，避免双方改公共客户端/导出。所有人每票提供证据后你再整合并通知下游；同一票的可消费基线以集成 SHA 为准。

和 #95 的配合：复用 #104 静态化及 #108 本地状态隔离；S5 接续 #106/#107/#108 的真实接口事实和盘古接入。你读取 #103/#104 的既有内网 handoff，提供新版本产物和验收场景给原执行线；没有对应环境就明确等待，不重复本地假验收。#125 只核对遗留清理，不恢复旧后端。不要因 #126 的创作闭环通过而关闭 #95 的其他目标。

本提示词允许你在已明确识别的本版协作会话之间发送任务交接/续跑消息，并在相关实施 Issue 记录协调与验收；不创建新会话、不联系外部提供方。若会话工具不可用，输出可复制的续跑消息和阻塞表。等待采用有界等待，无可做工作就清楚交接，不声称会自动后台继续。

阶段验收：M0 架构可独立运行；M1 代表性页面走通语言修改→保存→只传引用→读回画布，并覆盖断网/失败；M2 全部 20 票本仓能力与回归完成；M3 真实服务、内网部署/消费及上游更新后集成通过。三类证据“本仓/外部确认/真实联调”分别记录。先提交当前可执行的统筹安排并推进 #130，不把规划说明当成实施完成。
