工作项目：/Users/moon/Documents/Code/公司项目/DataDashboard。先只读读取 /Users/moon/Documents/Code/公司项目/DataDashboard/docs/plan/authoring-tickets-126/handoff/common.md 和 /Users/moon/Documents/Code/公司项目/DataDashboard/docs/plan/authoring-tickets-126/development-plan.md，再读取执行仓 AGENTS.md、领域词汇表、相关 ADR，以及 GitHub CCharlesMeng/MetricCanvas 的 #126 和本角色负责票的完整正文、最新评论与依赖。新 worktree 若没有未提交交接文件，从上述原始路径读取；保留原工作区及其他会话改动。遵循 common.md 的独立 worktree、共同基线、所有权、验收和等待约定。

你是 S4，负责独立生命周期 MCP。该会话在 S0 宣布 M0 READY 后启动。先核对 #128/#130 的精确修订、身份、幂等及服务消费契约已经验收进入共同基线；若没有则准备工具验收样例并列出具体缺口，不提前发明外部 HTTP。

执行 #138（T12）：提供保存、精确读取和历史的独立 MCP 入口；输入、基线、稳定逻辑操作标识、结果未知时的去重/查询及冲突语义与 Platform 共用需求。内容编辑算法、Java 持久化事务和提取规则不进你的模块。真实已知 HTTP 按 #105 资料适配；未提供能力通过明确端口替身验证，不能把 latest 当作旧修订或把 UUID 当作服务端幂等保证。

工具返回可交给前端的精确已保存草稿引用，完整文档通过可信程序通道处理，模型只见摘要/引用。验证重复请求、服务端已提交回执丢失、越权、基线冲突、精确读取和历史不可用。交付本地提交及工具契约给 S0/S1，解锁 #146。

#138/#140/#144 全部验收并集成后，参与 #145（T19），由 S1 主责整票，你只实施候选读取/修正/准备发布/确认发布的工具部分及外部适配。先与 S1/S0 冻结候选和确认关联，避免双方改同一共享客户端。验证未确认、旧候选、基线变化与过期结果无法发布。用户可以自然语言发起发布，但程序必须验证对当前候选的人工确认。

与 #95 配合：这是本版新生命周期工具，不恢复 #125 删除的旧服务端 MCP 和 Node 后端。Java/Relay 修改方案在 #130，真实服务事实沿 #105/#106；你不改外部服务，也不联系提供方。自己的工具部分完成后交 S1 做组合验收，不独立宣称 #145 整票完成。
