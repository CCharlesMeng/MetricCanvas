# #105 / #106 接口事实对账与当前提问稿

> **2026-09-10 用户决定：** #105 记录/修订关系及 #106 技能路由、回调等事实追问暂挂，等待实际联调；以下问题作为待核清单，不再向用户重复 grill。#105/#106 均未验收关闭。目录等已明确接入与 #108 本地页面状态逻辑可继续，未知接口不作能力否定。

日期：2026-09-09。状态：资料与本仓消费者已核对，缺口尚待答复。本文提问稿未发送提供方，不是提供方能力结论。

产品裁决沿用 ADR-0070、ADR-0077。#105 消费外部 Java 接口；#106 消费盘古实例 API。用户要求同一维度批量提问，先讨论 #105 的读取与寻址，再讨论其他维度。已提问但未答复的内容保持未知，不从“开始推进”推导接口答案。

## #105：2026-09-10 更新后的事实与执行边界

用户已明确：这是已经实现的服务，并提供了更新资料。[本次粘贴原文](wayfinder-105-provider-api-2026-09-10.txt)原样保存；粘贴文本缩进与 Markdown 格式混杂，不作为可直接生成代码的有效 YAML。前一版来源仍见 [旧 Swagger](https://github.com/CCharlesMeng/MetricCanvas/issues/105#issuecomment-5567257814)。已实现是用户提供的事实，本仓尚未完成真实服务消费验证。

本仓消费者为 `apps/platform/src/lib/page-assets-client.ts`，测试为 `apps/platform/tests/page-assets-client.test.ts`；旧服务端适配器在 `packages/server/page-assets-java/src/lifecycle.ts`。这些仍使用旧 `/pages` 协议，新接口共同前缀为 `/rest/cdi/cdinl2databuilderservice/v1`。

| 操作/维度 | 更新资料已明确 | 对前端的影响与剩余缺口 |
| --- | --- | --- |
| 目录 | `GET /user-page-metadata`，按登录用户读取；pageNo/pageSize 分页；needDefinition 默认 false；响应 page_metadata_list/total/page_no/page_size | 目录接口缺失已解除。页码从 1 开始，pageSize 最大 1000；两项都传合法正值可避免不分页的边界歧义 |
| 从目录打开页面 | 列表项同时返回 page_id、page_metadata_id、revision_id、revision_number | 可直接使用该列表项的 page_metadata_id 读取详情，无需自造映射。仅有 page_id 的直接链接仍需核查唯一性/解析语义；不能按第一页或第一条匹配猜测 |
| 详情 | `GET /user-page-metadata/{page_metadata_id}` 返回页面与修订标识 | 当前详情究竟对应最新内容还是不可变记录、资源 id 是否跨更新稳定，仍需事实确认 |
| 保存 | POST 集合路径新增；PUT 资源路径修改，revision_number 递增；PUT 必填 base_revision_id | 可以按明确请求实现新增/更新消费；重复提交及冲突后恢复尚无完整语义 |
| 精确修订 | 返回 revision_id，仍未列按 revision_id 查询的参数或路径 | 尚未解决旧修订完整内容的读取；不能以最新详情替代精确修订预览 |
| 返回结构 | page_metadata_definition 请求为 object、响应为 JSON 字符串；列表仅 needDefinition=true 返回内容 | 解析 JSON 并校验页面。原 PageRevision 中未给来源的字段不能伪造；列表不应无条件携带全部定义 |
| 业务结果 | PageMetadataResponse 与 PageMetadataListResponse 通过 allOf 包含 CommonRsp，retCode 为 string，说明 0 为成功；retDesc 为结果说明 | 成功响应要结合 HTTP 与业务码检查，不能仅看 HTTP 200。allOf 是字段组合，不是新增 data 外层；错误枚举、字段缺失行为仍需样例验证 |
| 身份 | 新版各操作列出 X-Auth-Token/X-Operator-Id，不再列 X-Workspace-Id | 不能沿用“三个头均由本接口强制要求”的旧结论。宿主额外请求头/Cookie 策略仍归宿主，不从文档省略推断禁止传该头 |

原先三项问题的状态：目录已明确；从目录到详情的标识已明确；page_id 独立寻址与历史修订读取仍待确认。旧客户端/测试不证明新服务已联调。DELETE 存在不等于本轮增加产品删除能力。

### 下一组核查：记录与修订的关系

用同一页面连续保存 R1、R2 的例子集中确认：

1. 更新后 page_metadata_id 是否保持不变？同一用户下一个 page_id 是否只对应一条记录，列表是否只列当前记录？这决定页面直达链接如何定位，不能擅自取第一条匹配。
2. 保存 R2 后，R1 的完整内容是否仍保留且可读取？若可读，请给调用方式；若不知道，登记待提供方确认，不把文档未列等同不支持。

这组是实际服务行为核查，不要求用户设计服务。下一组再核查保存/并发：重复提交、业务码、保存响应与后续读取的一致性。Swagger 仍写有最终一致性窗口，不能承诺保存后立即读取必定得到新修订。

已可按资料准备目录、详情、新增/更新、JSON 解析与业务码检查的适配；实际接线需运行配置及服务验证。完整客户端切换、精确修订验收与旧服务删除保持独立完成条件，不因目录补齐而全部关闭。

## #106：已核事实

来源为用户提供的本地《微前端子服务集成盘古助手第二实例 — 完整指南》：`/Users/moon/Downloads/粘贴的 markdown (1)。md(5)`。源码链接此前未能读取；本轮依据指南正文，不宣称已验证实际 SDK。

- 指南明确独立 instanceId、selector、`instance(id).renderChat(...)`、`destroy()` 与多实例隔离；父页面已经加载 loader 时复用，独立页面按标准流程加载。标准挂载方式不用再向用户作产品选择。
- `ask(query: PanguQuery): void`。本轮答复由消息呈现/回调验证，不能 await ask 返回一份结果；也不能由 void 推断没有答复。
- `renew` 表示新建会话再问；`referrer` 列出 agent/topic 字段；`skillInfo` 仅给类型名；`holdingSkill` 描述发送后保留 skill。实际受控路由组合与生效信号尚未给出。
- 回调和历史方法仅有名称及宽泛签名，缺真实 payload 和持久化契约。`autoRecover` 不等于分析会话检查点已经可恢复。
- 指南描述 streamEventStop 为无 stop 接口时的 abort、pingCheck 为 15 秒无返回中断兜底；不能据此推断服务端执行已取消。

### 当前对外提问稿（替代 2026-09-07 的旧 10 问措辞；编号仍为 1–10）

#### 挂载与部署事实（1–2）

1. 我们按指南使用独立 instanceId、复用已加载的 loader、卸载时 destroy。目标环境可用的客户端版本、loader 来源及实例配置是什么？是否有该版本特有的命名约束？请给可运行配置示例。
2. 我们将实例放入既有工作台左侧普通 DOM 容器。请给保持在该容器内、随容器调整且不拖动覆盖画布的配置示例；若存在 selector/嵌套/Shadow DOM 等限制，请指出。独立页面部署时 loader 的标准接入要求是什么？

#### 受控技能路由（3–4，优先解阻塞 #107）

3. 已确认需求是直接指定我们的受控技能，由技能承担问数编排。请提供实际 PanguQuery 示例：skillInfo/referrer/holdingSkill 的字段、组合与技能标识；新一轮与后续确认轮如何保持该路由。请说明实际跳过的通用流程，以及客户端只能引用标识、不能修改技能正文或工具权限的约束。
4. 客户端如何确认指定技能路由成功？未知标识、版本不匹配或回退通用问答时有什么明确错误/回调信号？请给成功和失败样例。

#### 回调、跨轮确认与恢复（5–8）

5. 请给 getCustomOnMessageEvent 及相关回调的实际签名和 payload，覆盖本轮开始、答复、结束、失败，以及 conversation/request/message 等标识。ask 返回 void 时，端侧如何识别本轮已答复并结束？我们首版只要求整体处理中与明确答复，不要求实时分步事件。
6. 我们每次 ask 一问一答，答复可以是确认问题；用户随后通过新一次 ask 回应。请给该两轮的完整示例：确认内容如何呈现、回应如何关联同一会话中正确的待确认项、由哪个接口/服务校验选择并恢复待办状态、确认前在哪里保证受约束操作未执行、过期或重复回应如何处理。若有专用确认接口，也请提供。另请单独说明是否支持无需模型重新解释的确定性条件修改；不要把“可再 ask”直接等同于该能力。
7. autoRecover/initialConversationId/customHistory 读写什么、接口返回什么、保存期限与权限是什么？请给刷新后读回待确认对话的示例，并说明它与发送服务、分析流程检查点的 id 和存储关系。聊天记录恢复与分析状态恢复分别由谁实现？
8. stopPanguMsg 的返回值代表什么？停止接收与服务端取消分别如何发生、如何确认？取消后的回调/迟到答复、断线、超时如何识别？请结合该版本说明 streamEventStop/pingCheck 的适用条件与重试建议。

#### 身份与发送适配（9–10）

9. getCurrentUser 的实际返回形状是什么？哪些字段参与鉴权/权限，哪些只用于显示？它与宿主登录态及 HIS 身份如何对应、续期？请给脱敏配置；无需提供凭据值。
10. 内部助手场景能否使用默认发送与会话创建？请给最小必要 adapter 配置。若必须实现 getCustomSendMessageRequest/getCustomConversationStart，请给完整输入输出及责任边界，明确客户端与集成方各自负责什么。

### 对答案的验收

- “支持”“按文档做”不能替代缺失的实际字段/调用示例；指南已明确的挂载方式按文档记录，不反复要求确认其存在。
- 第 6 问中，新一次 ask 是可接受承接方式；仍须证明待确认项关联与执行约束。没有专用确认接口不等于无法确认。
- 路由配置、确认与执行控制可能由不同提供方负责。若盘古只负责 UI/发送，记录 Relay 侧责任和待核接口，不把整条业务控制强加给客户端。
- #108 的完整产物读取仍须另核 Relay Page Artifact Adapter 与读取接口；不由本组聊天回调自动推导，也不从模型文本拼装页面。
- 未答复/未知/确认不支持分别登记。现有只出文本的下限不代表受控技能、身份或分析恢复已验收。

下一步：#105 目录事实已补齐，继续核查记录与修订关系；#106 优先获取 3/4/5/6 的真实示例。用户授权发送前，提问稿仅在本地供讨论。
