{
  "name": "metriccanvas",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter canvas dev",
    "build": "pnpm --filter canvas build",
    "test": "vitest run",
    "validate": "tsx packages/page/src/validate-cli.ts",
    "sync-catalog": "tsx packages/data-gateway/src/sync-catalog-cli.ts",
    "typecheck": "tsc --noEmit -p packages/page && tsc --noEmit -p packages/runtime && tsc --noEmit -p packages/data-gateway && tsc --noEmit -p packages/page/tsconfig.test.json && tsc --noEmit -p packages/runtime/tsconfig.test.json && tsc --noEmit -p packages/data-gateway/tsconfig.test.json",
    "check": "pnpm typecheck && pnpm --filter canvas check"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
