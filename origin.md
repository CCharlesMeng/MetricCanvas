# MetricCanvas 产品目标与边界

## 产品定位

MetricCanvas 是企业数据分析与可视化平台。业务需求通过受控查询或静态数据形成声明式看板页面，统一运行时将页面呈现为可交互的数据应用。

平台以看板页面为核心资产，以领域 DSL 作为页面表达，以不可变页面修订和人工确认发布保障可审计性。

## 业务目标

- 降低数据分析页面的创建和变更成本；
- 让 AI 能够生成、校验和修改结构化页面；
- 统一数据查询、页面描述、渲染和发布的边界；
- 提高页面、组件和数据契约的复用程度；
- 为页面变更、预览和发布提供可追溯记录；
- 使静态报告与动态查询页面使用同一页面协议和运行时。

## 系统能力

MetricCanvas 提供：

- 页面创作期的数据上下文和 Schema 元数据；
- NL2DQE 查询生成；
- 静态 `inline` 页面数据源；
- DQE `query` 页面数据源；
- 结果字段契约和显式查询字段映射；
- 页面领域 DSL 和校验器；
- 统一运行时、纯渲染组件与内化执行的 AI 总结组件；
- 页面修订、模板、发布租约和人工确认发布；
- Canvas、嵌入运行时和 Platform 管理界面。

## 数据场景

| 场景 | 页面数据源 | 运行行为 |
|---|---|---|
| 静态报告 | `inline` | 数据行随页面修订保存 |
| 动态分析 | `query` | 默认状态可使用内嵌初始行，其他状态通过数据网关执行 DQE |
| 组合页面 | `mixed` | 页面同时使用两类数据源 |

静态数据和 DQE 查询是并列的数据场景。页面组件通过数据快照消费数据，不感知数据来源。

## 系统边界

MetricCanvas 负责：

- 描述和校验看板页面；
- 在创作期提供受控数据上下文；
- 执行页面内已定义的 DQE 查询；
- 管理页面筛选状态和有限交互；
- 渲染页面并处理数据状态；
- 基于页面数据快照中明确声明的关联数据生成 AI 总结；
- 保存不可变页面修订；
- 管理预览、模板和发布。

MetricCanvas 不负责：

- 建设底层数据仓库或主题数据；
- 在运行时生成自然语言查询；
- 在页面中执行任意脚本、HTML 或计算表达式；
- 允许组件直接访问页面数据服务或数据网关；
- 从查询返回样例推断字段契约；
- 以查询成功或页面保存代替人工确认发布。

## 核心约束

1. 看板页面完整声明页面数据源、结果字段和组件绑定。
2. Schema 元数据只用于页面创作，不进入统一运行时。
3. 页面字段与外部查询字段通过 `queryField` 显式对应。
4. 页面筛选器通过 `filterBindings` 显式影响 DQE 查询。
5. 统一运行时确定性消费页面文档，不生成或改写页面中的查询定义；只根据已声明绑定和交互状态形成生效查询。
6. 纯渲染 View 不发起请求、不访问页面全局状态；AI 总结的 SSE 只存在于其垂直组件 Module，且只能读取已经形成的数据快照。
7. 页面修订不可变，发布指向精确修订。
