# 项目背景与架构调整目标

## 项目背景

MetricCanvas 面向企业数据分析与可视化场景。既有系统已经具备数据湖、主题数据、GraphQL 数据服务和 DQE 等数据执行能力，但看板主要以高代码页面交付：

- 查询、布局、状态与交互耦合在页面代码中；
- 页面开发、复用和变更成本高；
- 数据含义和查询约束分散；
- AI 难以稳定生成、校验和修改生产页面；
- 页面资产、查询资产和发布状态难以统一审计。

本项目不重新建设底层数据仓库。调整重点是查询生成、页面描述、运行时渲染和发布治理之间的边界。

## 调整目标

项目从“人工编写高代码页面”演进为：

> 以业务需求为起点，以受控查询定义提供动态数据，以声明式看板页面描述应用，以统一运行时渲染页面，以 AI 完成查询生成、页面组合与数据分析。

AI 生成可校验的查询候选和看板页面，不直接生成不可控的生产页面代码。平台负责：

- 数据上下文与 Schema 元数据；
- 查询权限、安全、资源限制和真实执行验真；
- 页面领域 DSL；
- 统一运行时；
- 不可变页面修订；
- 精确修订预览；
- 发布租约和人工确认。

## 数据场景

看板页面同时支持：

- `inline` 静态场景：数据随页面修订固化；
- DQE `query` 动态场景：页面内嵌查询定义，经数据网关执行；
- `mixed` 场景：同一页面组合静态和动态页面数据源。

DQE 是当前已实现的动态执行场景，不是平台唯一的数据形态。未来新增 SQL 等查询语言时，必须通过明确的执行协议和数据网关适配器接入。

## 核心原则

1. 看板页面是核心领域资产，不是源代码附件；
2. 页面完整声明结果字段契约；
3. Schema 元数据只服务创作期，不进入统一运行时；
4. 统一运行时确定性消费已发布页面修订；
5. 组件只消费数据快照，不发请求；
6. 页面字段与外部查询字段只通过显式映射衔接；
7. 页面不表达脚本、任意计算或查询文本模板；
8. 查询成功、页面保存和预览都不能代替人工确认发布。

## 最终方向

MetricCanvas 最终是一套 AI 原生数据应用平台：不同 Agent 可以共享同一数据上下文、页面 Schema、组件能力、校验器和发布流程，统一运行时则稳定呈现已经过治理的页面资产。
