# MetricCanvas(指标画布)

一套以业务需求为起点的 AI 原生数据分析与可视化平台:AI 通过 NL2SQL 或 NL2DQE 生成受治理的查询产物,声明式页面描述应用,统一运行时执行查询并渲染页面。

## Language

**数据服务 (Data Service)**:
建设在 GraphQL 之上的既有数据查询服务,是 DQE 查询的一种执行环境。新页面也可以使用受治理的 SQL 执行环境,因此数据服务不再是平台唯一数据入口。
_Avoid_: 表服务(旧称,2026-07-20 更名)、API 层

**数据服务仿真 (Data Service Sim)**:
开发期在本地按 GraphQL 数据服务的真实协议(apiQuery 信封、方言指令、元数据端点、错误码)提供数据的仿真服务。它只用于验证对应数据服务适配器,不代表 NL2SQL 路径。
_Avoid_: 假后端、本地 mock 服务

**DQE 仿真 (DQE Sim)**:
开发期独立运行并按 DQE 执行协议暴露真实 HTTP 端点的仿真服务。它接收查询定义的原始 `dsl_list`,保持查询项与结果项的位置对应,用于验证浏览器、数据网关、响应归一化和页面渲染的完整调用链路。
_Avoid_: 前端 mock、内存 fetch、组件造数

**数据上下文快照 (Data Context Snapshot)**:
NL2SQL/NL2DQE 生成时可使用的数据源、Schema、字段说明、关系、已验证查询示例、权限范围和执行约束的版本化副本。它不包含业务数据行,也不是预定义指标目录。
_Avoid_: 指标目录、元数据快照(旧模型名称)、业务数据快照

**Schema 元数据 (Schema Metadata)**:
数据上下文快照中描述执行环境、Schema、对象、字段、关系、权限、执行约束和已验证查询的机器可读部分。它只服务创作期,不包含业务数据行,不进入看板页面或统一运行时。
_Avoid_: 指标目录、运行时目录、样例数据

**NL2SQL**:
在数据上下文快照约束下,把已确认的自然语言分析需求转换为只读 SQL 查询候选的创作期能力。生成结果必须经过校验、真实执行和人工验收后才能成为查询产物。
_Avoid_: 裸 text-to-SQL、运行时自由生成 SQL

**NL2DQE**:
在数据上下文快照约束下,把已确认的自然语言分析需求转换为 DQE 查询候选的创作期能力。它与 NL2SQL 共享查询校验、查询产物和结果字段契约,只在目标查询语言与执行适配器上不同。
_Avoid_: 指标查询、数据服务直连

**查询产物 (Query Artifact)**:
由 NL2SQL、NL2DQE 或服务端组合生成的受治理查询资产,以稳定 id 承载同一性。它的每次成功保存形成一个查询修订。
_Avoid_: 指标、查询草稿、页面内 SQL

**查询修订 (Query Revision)**:
同一查询产物在一次成功保存时形成的、经过校验与真实执行且不可变的完整状态。它记录查询语言、目标执行环境、参数契约、结果字段契约和生成所用的数据上下文快照。
_Avoid_: 查询版本、可变查询

**查询定义 (Query Definition)**:
query 页面数据源内嵌的可执行查询,以 `language` 判别 SQL 或 DQE,以 `body` 保存外部查询协议的原始请求体。统一运行时按查询语言选择端点并原样提交 `body`,服务端校验后执行。
_Avoid_: DSL(易与领域 DSL 混淆)、查询执行引用

**参数契约 (Parameter Contract)**:
查询产物公开的受控输入集合,声明参数名称、类型、允许值域及是否必填。筛选状态只能通过参数契约影响查询,不能拼接或改写查询文本。
_Avoid_: 字符串模板、自由变量

**结果字段契约 (Result Field Contract)**:
查询产物对返回字段名称、标量类型、字段角色、标签、单位和可空性的稳定声明。它来自查询静态分析与执行验真,不从某次返回的数据行猜测。
_Avoid_: 指标目录、返回值样例、字段配置

**维度字段 (Dimension Field)**:
结果字段契约中用于分组、筛选、排序或类别展示的字段角色。它描述查询结果中的分析切分角度,不要求预先注册为平台资产。
_Avoid_: 维度资产、维度 code

**度量字段 (Measure Field)**:
结果字段契约中承载数值、比率、计数或其他可比较业务结果的字段角色。它由查询定义产生,不是预先治理并通过 code 引用的指标资产。
_Avoid_: 指标、指标 code、metric

**组合查询 (Composite Query)**:
在服务端执行多个查询定义,把其小结果集注册为临时命名表,再通过受限 SQL 得到最终结果的查询产物。中间数据不进入浏览器或页面文档。
_Avoid_: 页面计算、组件计算、JSON 计算 AST

**数据网关 (Data Gateway)**:
统一运行时的查询执行端口:查询定义与参数进,标准化数据行出。它按查询语言选择 SQL、DQE 或组合执行适配器,不感知看板页面和组件。
_Avoid_: 数据源封装、fetcher、组件查询接口

**看板页面 (Dashboard Page)**:
平台的聚合根与核心资产:以查询定义为动态数据基础的声明式数据应用单元。页面文档声明命名页面数据源、组件数据槽、嵌套内容分区和有限交互;同一性由页面 id 承载,统一运行时负责执行查询并呈现界面。
_Avoid_: 页面规格(旧称,2026-07-20 降级为文档形态,ADR-0007)、页面配置、JSON 配置、大屏

**页面修订 (Page Revision)**:
同一看板页面在一次成功保存时形成的、经过校验且不可变的完整状态。修订沿线性历史前进并完整保存其内嵌查询定义,与页面文档的 `schemaVersion` 无关。
_Avoid_: 页面版本(易与 `schemaVersion` 混淆)、草稿快照

**发布租约 (Publish Lease)**:
人工确认发布期间对看板页面当前最新修订的限时独占权。租约绑定页面与修订,活动期间页面不能产生新修订。
_Avoid_: 发布锁、审批锁

**页面模板 (Page Template)**:
供搭建新看板页面复用的受治理起点,有独立身份、修订、权限与发布状态;其内容引用一个已发布页面修订,不复制页面文档。
_Avoid_: 页面副本、JSON 模板

**模板修订 (Template Revision)**:
同一页面模板在一次成功保存时形成的不可变状态,包含模板的发现元数据以及所引用的精确已发布页面修订;来源看板页面后续变化不改变既有模板修订。
_Avoid_: 模板版本、页面副本

**目录隐藏 (Hidden from Catalog)**:
看板页面不再出现在目录发现结果中的展示状态,不是归档或删除;直接 URL 仍按权限访问当前发布修订。
_Avoid_: 归档、下线、删除

**领域 DSL (Domain DSL)**:
看板页面文档所使用的语言,以页面数据源、查询定义、结果字段契约、组件字段绑定和筛选联动为一等公民,自主定义并演进,不绑定外部 UI 协议。

**页面数据源 (Page Data Source)**:
看板页面中的命名数据集。`inline` 来源随页面固化数据行并完整声明结果字段契约;`query` 来源内嵌 SQL 或 DQE 查询定义及其受控参数绑定。组件只通过命名数据槽引用页面数据源,不携带查询或数据行。
_Avoid_: 渲染输入、快照信封、组件查询、数据源封装

**数据源依赖 (Data Source Dependency)**:
一个 query 页面数据源对另一页面数据源结果字段集合的声明式依赖,用于表达集合式级联取数。它独立于组件数据槽绑定和筛选状态;组件局部视图不改变依赖输入。
_Avoid_: 组件级联、接口串联、组件查询

**组件字段绑定 (Component Field Binding)**:
纯渲染组件从命名数据槽引用结果字段并声明当前视图展示格式的页面文档结构。绑定不改变结果字段契约或查询语义。
_Avoid_: 数据源格式、字段配置、组件查询

**查询字段映射 (Query Field Mapping)**:
页面数据源的稳定结果字段 id 与外部查询协议字段名之间的显式对应关系。组件和筛选状态只引用页面字段 id,数据网关负责把查询结果归一为这些 id;两侧字段名永不隐式同名匹配。
_Avoid_: 自动同名映射、组件直引 DQE 字段

**筛选绑定 (Filter Binding)**:
页面筛选器与某个查询定义中受控筛选目标之间的显式对应关系。它声明筛选状态作用于 DQE 维度或时间等语义位置,不使用 JSONPath、字符串模板或表达式。
_Avoid_: 默认字段同名、查询文本插值

**统一运行时 (Runtime)**:
读取看板页面,解析页面数据源并将其渲染为界面的引擎。页面只描述“是什么”,查询编排、状态管理和渲染行为全部由运行时决定;运行时不执行 NL2SQL/NL2DQE。
_Avoid_: 渲染器、前端框架

**渲染通道 (Render Channel)**:
消费同一个看板页面并将其变成可见界面的一条路径。当前唯一通道是统一运行时;未来 A2UI 适配器是第二条通道。页面不感知通道的存在。

**纯渲染组件 (Pure Component)**:
运行时组件集中的组件形态:只通过 props 接收已解析的数据行、结果字段契约与展示配置,只通过事件上抛交互;不发请求、不碰全局状态、不管理加载态。组件集由平台治理,看板页面只引用已登记的组件类型,不携带组件代码。
_Avoid_: 智能组件、容器组件

**筛选状态 (Filter State)**:
页面级共享的筛选条件集合。筛选器写入它,query 页面数据源把订阅值绑定到查询定义的参数契约;组件 action 可回写它以实现联动下钻。联动只通过筛选状态传递,组件间不直接连线。

**跨页下钻 (Drill-through)**:
从一个页面的组件 action 跳转到另一个页面并携带筛选条件的导航行为,在页面文档中以 `props.actions[].navigate` 声明,物理载体是 URL。区别于页内下钻(回写筛选状态)。

**生效查询 (Effective Query)**:
查询定义与筛选状态按参数契约绑定后形成的一次确定性执行请求。它包含查询语言、查询原文和参数,不包含自然语言生成动作。

**数据快照 (Data Snapshot)**:
统一运行时按组件数据槽分发的数据包,含数据行与数据状态(就绪/加载/错误/空)。`inline` 页面数据源同步产生终态数据快照,`query` 页面数据源经数据网关执行后产生动态数据快照;组件不感知来源和取数过程。
_Avoid_: 查询产物、响应信封

## Retired Language

**查询执行引用 (Query Execution Reference)**:
旧模型中由 query 页面数据源持有、指向精确查询修订的引用。当前模型改为页面内嵌查询定义并由浏览器直接提交原文。

**指标 (Metric)**:
旧模型中由数据服务预先定义并通过 code 引用的业务度量。新模型不把它作为页面构建前置资产;需要描述查询结果中的数值语义时使用“度量字段”。

**指标履约 (Metric Fulfillment)**:
旧模型中围绕指标缺口、DP 候选、指标需求队列和数据服务目录验真的流程。新模型以查询生成失败、查询校验失败或数据上下文不足作为可操作阻断,不再进入指标履约流程。
