# MetricCanvas 领域词汇表

MetricCanvas 是以看板页面为核心资产的 AI 原生数据分析与可视化平台。本文定义当前系统使用的领域术语。

## 数据与查询

**DQE**:
看板动态数据使用的查询语言与执行协议。查询定义保存 DQE 原始请求体，数据网关负责提交请求并归一化结果。
_Avoid_: 页面查询 DSL、组件查询协议

**DQE 仿真 (DQE Sim)**:
开发环境中的 DQE HTTP 服务。它接收 `dsl_list`，保持查询项与结果项的位置对应，用于验证数据网关、统一运行时和页面渲染。
_Avoid_: 前端 mock、组件造数

**数据上下文快照 (Data Context Snapshot)**:
页面创作期可使用的数据源、Schema、字段、关系、已验证查询、权限范围和执行约束的不可变版本。
_Avoid_: 页面数据、运行时数据快照

**Schema 元数据 (Schema Metadata)**:
数据上下文快照中描述执行环境、Schema、对象、字段、关系、权限、执行约束和已验证查询的机器可读内容。Schema 元数据不包含业务数据行。
_Avoid_: 页面元数据、运行时目录、样例数据

**指标条目 (Metric Entry)**:
数据上下文快照中描述一个业务指标的发现条目，包含业务名、别名、口径说明、单位、是否为比率、可加性与时间聚合方式、可用维度以及所属业务域。指标条目只用于创作期的能力发现与口径锚定，不进入看板页面协议，也不是形成查询的强制前置。
_Avoid_: 指标目录、预定义指标、指标履约、页面指标引用

**业务域 (Business Domain)**:
指标条目所属的业务范围，用于把数据能力发现的检索面从全量收窄到一到两个域。业务域是路由标签，各域共用同一个执行环境；它不表达数据隔离、端点差异或凭据边界。
_Avoid_: 执行环境、数据源、租户

**可加性 (Additivity)**:
指标条目声明的聚合能力：可加、半可加或不可加，并附跨时间应求和、取均值还是取期末值。可加性缺失时，该指标不得跨粒度聚合。
_Avoid_: 聚合函数、统计方式

**临时口径 (Ad-hoc Definition)**:
未命中指标条目时由创作期现场生成的计算口径，以查询定义内的 `formula` 表达。临时口径可用于问数、探索与报告，呈现时必须与已定义指标视觉可区分；沉淀为 Data App 时须由用户显式接受其无人负责，且该事实随页面持久化。
_Avoid_: 自定义指标、派生指标、计算字段

**指标需求条目 (Metric Gap Entry)**:
检索不到合适指标时落库的结构化记录，包含问题原文、检索词、最接近的候选与口径差异、临时口径、期望维度与粒度、所属业务域和出现次数。它是数据侧新建指标的需求来源，不是阻塞创作期的状态。
_Avoid_: 错误日志、`METRIC_GAP` 状态、指标申请单

**已验证查询 (Verified Query)**:
数据上下文快照中经人工背书的业务问题与其正确查询的对应关系。它为问数和页面搭建提供口径依据与生成参考；来自问数反馈的查询先成为候选，经人工背书后才进入快照。
_Avoid_: 查询产物、样例查询、查询缓存

**取数单元 (Data Request Unit)**:
创作期用业务语言描述「要什么数」的单元：一组指标、切分维度、时间范围与粒度、筛选条件，以及期望的结果形状。它是数据设计阶段的操作对象，可在未形成任何查询之前存在与修改；查询定义与结果字段契约都是它经真实执行后的派生物，不在这一层手写。一个取数单元最终对应看板页面的一个页面数据源。（暂命名，见 ADR-0032）
_Avoid_: 数据集资产、查询产物、计算数据集、页面数据源

**NL2DQE**:
在数据上下文约束下，把自然语言分析需求转换为 DQE 查询候选的创作期能力。指标名、维度名、维度取值和时间粒度取自数据上下文的可枚举集合；派生计算允许生成 `formula` 表达式，但须留痕并受可加性校验约束。查询候选经过清单校验和真实执行后进入看板页面。
_Avoid_: 运行时生成查询、自由文本查询

**查询定义 (Query Definition)**:
`query` 页面数据源内嵌的可执行查询。查询定义以 `language` 判别所属协议，承载该协议的原始请求体和可选筛选绑定；`language: "dqe"` 保存 DQE 原始请求体。
_Avoid_: 查询引用、组件查询

**结果字段契约 (Result Field Contract)**:
页面数据源对稳定页面字段 id、字段类型、字段角色、标签、单位和可空性的声明。查询数据源同时声明外部响应字段映射；嵌套明细字段还就地声明数组项契约，语义 HTML 明细字段声明受控富内容类型。
_Avoid_: 返回值样例、组件字段配置

**嵌套明细字段 (Nested Detail Field)**:
结果字段契约中角色为 `detail`、类型为 `recordList` 的一层对象数组。数组项的字段契约和查询字段映射必须就地显式声明，项属性只允许标量；单个结果行的每个嵌套明细字段最多 100 项。纯渲染组件只有通过专用属性显式绑定时才能消费。
_Avoid_: 任意 JSON、多层嵌套、组件自行解析 DQE 原始字段

**受控语义 HTML (Controlled Semantic HTML)**:
由有限标签和语义类表达文本结构与业务方向的富内容。内容不携带具体视觉样式，未通过治理契约的内容失败关闭。
_Avoid_: 任意 HTML、原始 HTML、富文本字符串

**语义 HTML 明细字段 (Semantic HTML Detail Field)**:
结果字段契约中角色为 `detail`、类型为 `semanticHtml` 的受控语义 HTML。DQE 只返回允许的标签、文本和表达内容含义的语义类；数据网关保持字符串不透明。单个值最多 64000 字符。
_Avoid_: 任意 HTML、原始 HTML 注入、数据层 CSS、富文本字符串

**维度字段 (Dimension Field)**:
结果字段契约中用于类别、时间、分组、筛选或排序的字段，角色为 `dimension`。
_Avoid_: 维度资产

**度量字段 (Measure Field)**:
结果字段契约中承载数值、比率、计数或其他可比较结果的字段，角色为 `measure`。
_Avoid_: 组件数值

**查询字段映射 (Query Field Mapping)**:
稳定页面字段 id 与 DQE 输出字段名之间的显式对应关系，由查询数据源字段的 `queryField` 声明。
_Avoid_: 自动同名映射、组件直引 DQE 字段

**筛选绑定 (Filter Binding)**:
页面筛选器与 DQE 查询维度或时间目标之间的显式对应关系，由查询定义的 `filterBindings` 声明。
_Avoid_: 查询文本插值、隐式字段匹配

**生效查询 (Effective Query)**:
查询定义、结果字段映射和当前筛选值组成的一次确定性 DQE 执行请求。
_Avoid_: 自然语言生成请求

**查询分页 (Query Pagination)**:
表格对查询页面数据源当前结果窗口的浏览方式；页码变化通过 DQE `order.offset` 和 `order.limit` 形成新的生效查询，不切分已有数据快照。
_Avoid_: 本地分页、表格切片

**数据网关 (Data Gateway)**:
统一运行时的查询执行端口。生效查询进入数据网关，标准化页面数据行和可选总条数返回统一运行时。数据网关按查询定义的协议分发到对应执行适配器；组件与浏览器不得直连远程数据端点。
_Avoid_: 组件查询接口、页面仓储

**相对时间表达 (Relative Time Expression)**:
时间范围筛选器可声明的结构化默认值，由粒度单位、区间描述和锚点组成，并显式声明是否包含当前未完成周期。求值发生在服务端取数编排期，一次页面加载内共享同一求值时刻。它是声明式数据，不是表达式字符串。
_Avoid_: 时间表达式、日期宏、相对时间字符串

## 页面

**看板页面 (Dashboard Page)**:
平台的聚合根与核心资产。页面文档声明页面数据源、筛选状态、内容分区、组件数据槽和有限交互，同一性由页面 id 承载。
_Avoid_: 页面配置、JSON 配置、大屏

**页面元数据 (Page Metadata)**:
描述一个看板页面的完整声明式文档。顶层结构包含 `schemaVersion`、`id`、`meta`、`dataSources`、`filters` 和 `sections`。
_Avoid_: Schema 元数据、页面描述片段

**领域 DSL (Domain DSL)**:
看板页面使用的声明式语言。领域 DSL 以页面数据源、结果字段契约、筛选状态、内容分区、组件字段绑定和 action 为基本结构。

**内容分区 (Section)**:
看板页面中直接拥有一组组件的布局单元，可选标题与分区容器。分区内组件按 12 列网格自动流式排布，网格列数不属于页面文档。
_Avoid_: 布局容器、组件分组、区块

**分区容器 (Section Container)**:
内容分区声明的外观档位，是分区外观的唯一真源，封闭且命名表现中性（`plain`/`panel`/`card`），缺省为通用看板外观。外观不由组件组合或组件 variant 推断，档位也不是自由样式轴。
_Avoid_: 分区外壳、分区 variant、布局配方、页面主题、样式 DSL

**页面数据源 (Page Data Source)**:
看板页面中的命名数据集。`inline` 数据源保存静态数据行；`query` 数据源保存 DQE 查询定义。两类数据源都在数据源当前位置声明完整结果字段契约。
_Avoid_: 组件查询、数据快照

**静态页面数据源 (Inline Page Data Source)**:
数据行随页面文档保存的页面数据源，`source.type` 为 `inline`。
_Avoid_: 本地 mock、查询缓存

**查询页面数据源 (Query Page Data Source)**:
保存查询定义并可保存内嵌初始行的页面数据源，`source.type` 为 `query`；需要取数时由统一运行时通过数据网关执行。
_Avoid_: 组件请求、服务端页面

**内嵌初始行 (Embedded Initial Rows)**:
查询页面数据源可随页面修订保存的默认查询状态 DQE 原始结果行，字段键使用 DQE 输出字段名，分页表格的第一页也包含在内。页面文档解析时通过查询字段映射归一化为稳定页面字段；存在且当前状态与默认查询状态一致时直接用于首次呈现且不触发后台查询，不存在或状态不一致时立即执行查询。
_Avoid_: 首屏数据、查询缓存、静态页面数据源

**页面修订 (Page Revision)**:
看板页面成功保存时形成的不可变完整状态。修订沿线性历史前进；查询页面修订记录创作所依据的数据上下文版本。
_Avoid_: `schemaVersion`、可变草稿

**发布租约 (Publish Lease)**:
人工确认发布期间对看板页面当前修订的限时独占权。租约绑定页面和修订。
_Avoid_: 审批状态、长期锁

**页面模板 (Page Template)**:
用于创建看板页面的受治理起点。模板具有独立身份、修订、权限和发布状态，并引用一个精确的已发布页面修订。
_Avoid_: 页面副本、JSON 复制

**模板修订 (Template Revision)**:
页面模板成功保存时形成的不可变状态，包含模板发现信息和所引用的页面修订。
_Avoid_: 页面修订

**目录隐藏 (Hidden from Catalog)**:
看板页面不出现在页面目录发现结果中的展示状态。直接 URL 仍按权限访问其发布修订。
_Avoid_: 删除、归档

## 分析形态与沉淀

**问数 (Ask)**:
用户以一句业务问题得到一个分析结果的形态。数据能力发现、查询生成与验真都在创作期完成，产出临时页面态交由统一运行时渲染，不产生页面修订。
_Avoid_: 对话式 BI、运行时生成查询、聊天机器人

**探索 (Explore)**:
在问数结果之上通过多轮自然语言做数据校准、筛选调整和展示调整的形态。每轮是对临时页面态的定向增量修改，用户未提及的显式设置保持不变。
_Avoid_: 自助分析、即席查询沙箱、页面搭建工作台

**分析会话 (Analysis Session)**:
一次问数与探索过程的服务端记录，由会话 id 与步骤事件流组成。每步记录问题原文、路由域、候选与选中指标、临时口径、生效查询、执行结果摘要、意图与组件选择和失败分类；不记录完整对话文本与模型 prompt。会话按保留期清理，仅平台管理员与本人可见。
_Avoid_: 对话历史、聊天记录、页面草稿

**口径卡 (Scope Card)**:
执行前向用户展示的完整生效范围，列出业务域、指标或临时口径、时间范围与粒度、筛选条件，并在候选存在歧义时并列 top-k 候选及其口径差异。口径卡首先是一次消歧，其次才是一次确认。
_Avoid_: 确认弹窗、查询预览

**数据校准 (Data Calibration)**:
探索中修正查询口径的轮次，例如排除某类客户或更换统计范围。校准改变生效的查询定义并重新执行；它不是对已有数据行的本地过滤，经人工背书后可成为候选已验证查询。
_Avoid_: 本地筛选、结果后处理、页面筛选状态

**报告 (Report)**:
一次性分析结论的交付形态。报告保留查询定义与采集时点的内嵌初始行且不声明筛选绑定，因此默认状态下不重新查询，内容随页面修订冻结在采集时点，同时保住口径溯源。报告的时间范围是绝对区间。
_Avoid_: 快照看板、导出文件、离线副本

**Data App**:
长期运行的看板页面形态。Data App 是经人工确认发布的页面修订，具备修订历史、发布治理和稳定访问入口，时间范围使用相对时间表达以随周期滚动。
_Avoid_: 报表、大屏、页面副本

**监测 (Monitor)**:
对稳定指标口径持续求值并在异常时主动提示的形态。监测依赖调度、基线与通知，当前不建设。
_Avoid_: 告警看板、定时报表

**临时页面态 (Transient Page State)**:
问数与探索过程中形成的完整页面文档。临时页面态通过页面校验并由统一运行时直接渲染，使用临时页面 id，不进入页面仓储、不产生页面修订、不出现在页面目录、不参与发布治理。
_Avoid_: 草稿修订、可变草稿、预览页面

**沉淀 (Promote)**:
把临时页面态显式转为长期资产的动作。沉淀为 Data App 时保存为页面修订，页面时间须为相对时间表达，含临时口径时须由用户显式接受；沉淀为报告时保留查询定义与内嵌初始行并去除筛选绑定。沉淀只由用户显式发起，不由问数或探索自动触发。
_Avoid_: 自动保存、另存为看板、发布

## 运行与渲染

**统一运行时 (Runtime)**:
加载、校验和渲染看板页面的引擎。统一运行时解析页面数据源，执行查询，管理筛选状态，并向组件分发数据快照。
_Avoid_: 页面搭建 Agent、组件库

**页面仓储 (Page Repository)**:
统一运行时按页面 id 加载页面文档和页面清单的端口。加载结果在通过页面校验前视为不可信数据。
_Avoid_: 数据网关

**数据快照 (Data Snapshot)**:
统一运行时以页面数据源 id 为唯一键保存的数据包，状态为加载、就绪、错误或空，在就绪或空结果态携带标准化数据行和可选总条数，在错误态携带查询错误分类与脱值消息。组件数据槽在渲染时投影到数据快照，不复制数据行。
_Avoid_: 页面数据源、DQE 响应信封

**查询错误分类 (Query Error Code)**:
数据网关执行生效查询失败时的稳定封闭分类，取消、需要登录、无权限、超时、查询被拒绝、上游失败与结果字段契约不匹配各有独立分类。分类随错误贯通数据快照错误态、组件呈现与嵌入事件，消费方按分类决定重试、重新登录或展示失败；错误只携带脱值消息与结构化事实，不携带查询结果、筛选值、Secret 或上游响应正文。
_Avoid_: 错误字符串解析、HTTP 状态码分支、运行时平行错误分类

**组件数据槽 (Component Data Slot)**:
普通数据组件声明的命名数据入口。数据槽引用页面数据源，统一运行时在渲染时把对应数据快照投影给组件。
_Avoid_: 查询参数、全局状态

**组件标题 (Component Title)**:
组件自身的可见标题，统一由 `props.title` 声明。组件能力目录明确每种组件的标题是必填、可选或不支持。
_Avoid_: `heading`、组件外散落标题、场景名称

**组件字段绑定 (Component Field Binding)**:
组件从数据槽引用页面字段并声明当前视图格式的页面结构。字段绑定不改变结果字段契约或查询语义。
_Avoid_: 查询字段映射

**纯渲染组件 (Pure Component)**:
只消费数据快照、字段契约和展示属性，并通过事件上抛交互的组件。纯渲染组件不发起查询、不访问页面全局状态、不管理数据加载。
_Avoid_: 智能组件、页面控制器

**行对齐 (Row Alignment)**:
统一运行时的渲染不变量：同一视觉行内同类型、同 variant 且具备对齐能力的组件，内部行轨按序号等高对齐，堆叠后自动复原。对齐经由显式契约协作，组件自己测量并写回自己的 DOM；它不进入页面文档，也不是某类组件的特例。
_Avoid_: 行高同步 family、排行行高特例、跨组件 DOM 操作

**AI 总结组件 (AI Summary Component)**:
基于明确声明的关联数据生成流式文本的垂直组件 Module。它在自身边界内完成请求组装、生成会话和私有 SSE 协议适配，再把 AI 总结快照交给纯渲染 View；它不是页面数据源或数据网关能力。它不是摘要的默认形态；只有需求明确声明运行时 SSE 动态生成时才使用，其余摘要由后端随页面文档返回并使用 `text` 组件。
_Avoid_: 第三种页面数据源、通用 AI Runtime、智能数据网关

**关联数据 (Related Data)**:
AI 总结组件对既有页面数据快照的显式只读引用，声明页面数据源、业务说明以及允许发送的字段与术语。关联数据不创建数据副本，也不是组件数据槽。
_Avoid_: `dataQuestions`、`contentSources`、AI 查询参数

**AI 总结快照 (AI Summary Snapshot)**:
统一运行时按组件 id 隔离的生成结果状态，包含加载、流式文本、就绪、空或错误。它由 AI 总结组件会话产生，不写回页面数据快照。
_Avoid_: 页面数据快照、SSE 响应对象、页面 DSL 的 `data`

**筛选状态 (Filter State)**:
页面级共享筛选值集合。筛选控件和组件 action 写入筛选状态，查询数据源通过筛选绑定读取相关值。

**页内下钻 (Drill-down)**:
组件 action 写入页面筛选状态并触发相关查询数据源重新执行的交互。

**跨页下钻 (Drill-through)**:
组件 action 导航到另一个页面，并通过 URL 携带或设置筛选条件的交互。

## 发布与消费

**发布修订 (Published Revision)**:
经过人工确认并作为页面当前运行态内容的精确页面修订。

**精确修订预览 (Exact Revision Preview)**:
使用指定页面修订进行渲染的预览。预览内容不跟随页面后续保存而变化。

**人工确认发布 (Human-confirmed Publish)**:
发布租约持有者确认精确页面修订成为发布修订的操作。查询成功、页面保存和预览不等同于发布。
